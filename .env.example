# MongoDB connection string (required)
# Use authSource=admin for Atlas. URL-encode special chars in password (@ → %40)
MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/Backend?retryWrites=true&w=majority&authSource=admin

# JWT secret for signing tokens (required)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
